# 🪙 OBUHOV COIN - Криптовалютна платформа

## 📋 Опис проєкту

**OBUHOV COIN** - це повнофункціональна криптовалютна платформа з власною валютою, системою обміну, ринковими ордерами та централізованою базою даних.

### 🎯 Основні можливості

- 💰 **Двовалютна система**: OBUHOV монети + USD
- 🔄 **Миттєвий обмін**: Купівля/продаж за поточним курсом
- 📊 **Ринкові ордери**: Виставлення заявок на купівлю/продаж
- 💸 **Переказ коштів**: Між користувачами з комісією 1%
- 🔐 **Система авторизації**: Багатокористувацький доступ
- 👤 **Профіль користувача**: Історія транзакцій, активні ордери
- 🛡️ **Адмін-панель**: Управління курсом, перегляд всіх користувачів
- 🌐 **Централізована БД**: Синхронізація між пристроями
- 📡 **Публічний сервер**: Доступ з будь-якого пристрою

---

## 🏗️ Архітектура

### Frontend (Клієнт)
- **HTML/CSS/JavaScript** - Vanilla JavaScript (без фреймворків)
- **API Client** (`assets/scripts/api.js`) - REST API обгортка
- **Сторінки**:
  - `index.html` - Головна сторінка + логін
  - `profile.html` - Профіль користувача
  - `admin.html` - Панель адміністратора

### Backend (Сервер)
- **Node.js + Express** - REST API сервер
- **SQLite** - База даних (`obuhov_coin.db`)
- **CORS** - Підтримка cross-origin запитів

### База даних (SQLite)

#### Таблиця `users`
```sql
- id INTEGER PRIMARY KEY
- username TEXT UNIQUE
- password TEXT
- coins REAL (баланс OBUHOV)
- usd REAL (баланс USD)
- frozenCoins REAL (заморожені OBUHOV в ордерах)
- frozenUSD REAL (заморожені USD в ордерах)
- isAdmin INTEGER (0 або 1)
- registeredAt TEXT (ISO timestamp)
```

#### Таблиця `transactions`
```sql
- id INTEGER PRIMARY KEY
- type TEXT (transfer/buy/sell/market_trade/rate_change)
- fromUser TEXT
- toUser TEXT
- coins REAL
- usd REAL
- fee REAL
- description TEXT
- timestamp TEXT
```

#### Таблиця `market_orders`
```sql
- id INTEGER PRIMARY KEY
- username TEXT
- type TEXT (buy/sell)
- coins REAL
- pricePerCoin REAL
- status TEXT (active/completed/cancelled)
- createdAt TEXT
```

#### Таблиця `settings`
```sql
- key TEXT PRIMARY KEY
- value TEXT
```

---

## 🚀 Запуск проєкту

### Крок 1: Встановлення залежностей
```powershell
install.bat
```
або
```powershell
npm install
```

### Крок 2: Налаштування firewall (для публічного доступу)
```powershell
firewall-setup.bat
```
або вручну:
```powershell
netsh advfirewall firewall add rule name="OBUHOV COIN Server" dir=in action=allow protocol=TCP localport=28015
```

### Крок 3: Запуск сервера
```powershell
start.bat
```
або
```powershell
node server.js
```

Сервер запуститься на:
- **Локально**: http://localhost:28015
- **Публічно**: ----------------------

---

## 🎮 Використання

### Для звичайних користувачів

1. **Реєстрація**:
   - Відкрити сайт
   - Клікнути "Увійти" → "Реєстрація"
   - Створити акаунт (отримаєте 0 OBUHOV та $0)

2. **Купівля монет**:
   - Увійти в профіль
   - Таб "Купівля/Продаж"
   - Ввести кількість USD → Купити

3. **Продаж монет**:
   - Таб "Купівля/Продаж"
   - Ввести кількість OBUHOV → Продати

4. **Переказ**:
   - Таб "Переказ"
   - Вказати username отримувача
   - Вказати суму OBUHOV
   - Надіслати (комісія 1%)

5. **Ринкові ордери**:
   - Таб "Ринок"
   - Створити ордер на купівлю/продаж
   - Вказати кількість і бажану ціну
   - Ордер буде виконаний автоматично, якщо знайдеться підходяща пропозиція

### Для адміністратора

Дефолтний адмін акаунт:
- **Username**: `admin`
- **Password**: `admin`
- **Початковий баланс**: $10,000

**Можливості адміна**:
- ✅ Зміна курсу валюти
- ✅ Перегляд всіх користувачів
- ✅ Перегляд усіх транзакцій
- ✅ Видалення користувачів

---

## 💱 Система валют

### Курс обміну
- За замовчуванням: **1 OBUHOV = $2.65**
- Адміністратор може змінити в будь-який час

### Комісії
- **Переказ між користувачами**: 1%
- **Купівля OBUHOV**: 0.5%
- **Продаж OBUHOV**: 0.5%
- **Ринкові угоди**: 0.3%

### Ринкові ордери
- **Buy Order** (Купівля): Заморожує USD до виконання
- **Sell Order** (Продаж): Заморожує OBUHOV до виконання
- Автоматичне виконання при співпадінні цін
- Сортування: Buy від найвищої ціни, Sell від найнижчої

---

## 📡 API Endpoints

### Користувачі
```
POST   /api/register          - Реєстрація
POST   /api/login             - Авторизація
GET    /api/user/:username    - Отримати дані користувача
GET    /api/users             - Всі користувачі (адмін)
PUT    /api/user/:username    - Оновити дані користувача
DELETE /api/user/:username    - Видалити користувача (адмін)
```

### Транзакції
```
GET    /api/transactions      - Всі транзакції
POST   /api/transactions      - Додати транзакцію
```

### Ринкові ордери
```
GET    /api/orders            - Всі активні ордери
POST   /api/orders            - Створити ордер
DELETE /api/orders/:id        - Видалити ордер
```

### Налаштування
```
GET    /api/settings/exchangeRate  - Поточний курс
PUT    /api/settings/exchangeRate  - Оновити курс (адмін)
```

### Перевірка
```
GET    /api/health            - Статус сервера
```

---

## 🔐 Безпека

### Клієнт
- ✅ Всі запити йдуть через API
- ✅ Немає даних користувачів у localStorage
- ✅ Session тільки зберігає currentUser (username)
- ✅ Перевірка авторизації на кожній сторінці

### Сервер
- ✅ CORS налаштований
- ✅ Валідація всіх вхідних даних
- ✅ Перевірка прав адміністратора
- ✅ Логування всіх операцій
- ✅ Транзакції записуються в БД

### База даних
- ✅ SQLite з локальним файлом
- ✅ Автоматичне створення таблиць
- ✅ UNIQUE constraint на username
- ✅ Збереження історії всіх операцій


### Frontend
- HTML5, CSS3
- Vanilla JavaScript (ES6+)
- Async/Await
- Fetch API

### Backend
- Node.js v14+
- Express.js
- SQLite3
- CORS

### Інструменти
- npm
- Windows Batch Scripts
- Git

---

## 📊 Моніторинг

### Логи сервера
Сервер виводить у консоль всі операції:
```
🚀 Server running on:
📍 Local:  http://localhost:28015
🌍 Public: http://---------------

[POST /api/register] Реєстрація: testuser
[POST /api/login] Логін: testuser
[POST /api/transactions] Нова транзакція: transfer
[GET /api/orders] Завантаження ордерів
```

### Перевірка бази даних
```powershell
sqlite3 obuhov_coin.db
```
```sql
SELECT * FROM users;
SELECT * FROM transactions ORDER BY timestamp DESC LIMIT 10;
SELECT * FROM market_orders WHERE status='active';
SELECT value FROM settings WHERE key='exchangeRate';
```

---

## 🐛 Відлагодження

### Проблема: Сервер не запускається
**Рішення**:
1. Перевірити, чи порт 28015 вільний:
   ```powershell
   netstat -ano | findstr :28015
   ```
2. Встановити залежності:
   ```powershell
   npm install
   ```

### Проблема: Не можу підключитись з іншого пристрою
**Рішення**:
1. Запустити `firewall-setup.bat` як адміністратор
2. Перевірити IP:
   ```powershell
   ipconfig
   ```
3. Переконатись, що сервер запущений на `0.0.0.0:28015`

### Проблема: Дані не синхронізуються
**Рішення**:
1. Перевірити підключення до API:
   ```javascript
   const status = await API.checkConnection();
   console.log(status);
   ```
2. Відкрити консоль браузера (F12) і шукати помилки
3. Перевірити логи сервера

---

## 📝 Changelog

### v2.0.0 - Міграція на API/Database ✅
- Повна міграція з localStorage на API
- Централізована SQLite база даних
- Публічний сервер на -----------------
- Логування всіх операцій
- Синхронізація між пристроями

### v1.0.0 - Початкова версія
- Базова система авторизації
- LocalStorage для зберігання даних
- Купівля/продаж валюти
- Ринкові ордери
- Адмін-панель

---


## 🎉 Подяки

Дякуємо всім, хто використовує OBUHOV COIN! 🪙

---

**🚀 Готові до торгівлі? Запускайте `start.bat` і вперед! 💰** 🚀💎

Ласкаво просимо в Obuhov Coin — стильний сайт розроблений 2 сеніорами 5 розряду для крипто коіна "обухов" з сучасним дизайном.

## ✨ Основні переваги

- Сучасний привабливий UI дизайн 
- Коін який 100% не скам (купіть пж)
- Багато пасхалок 
- Секретні пасхалки
- Ну і сам коін (толька картінка) (Ви не захотіли ставить автомат за токен на біржі)
- А ну і канешно командна робота над проектом і коміти

## 🎯 Що робить цей проєкт

Ми створюємо вебсайт для криптовалюти з такими фічами:
- Красивий дизайн (ну ряльно)
- Не скам токент (чесна)  
- Команда профі (3 сеніора)
- Платна підписка на новини (не факт шо будуть приходити)
- Підписка на скам сервіси (Тіки по номеру карточки)
- Реклама коіна

## 🧩 Технології та інструменти

- HTML5, сучасний CSS
- Node js тоже есть
- JavaScript (vanilla)
- Анімації: CSS + js
- DataBase SQLite
- Link to mono bank buy

## 📬 Контакти

Створено з ❤️ від студентів Лисака Максима та Бершова Костянтина.  
Питання, колаборації або ідеї — писати на abetik.dev@gmail.com або maksim.lysak2010@gmail.com.